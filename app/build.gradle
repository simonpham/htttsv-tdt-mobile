import java.text.SimpleDateFormat

apply plugin: 'com.android.application'
apply plugin: 'realm-android'

def buildTimestamp() {
    return (System.currentTimeMillis()/1000).toInteger()
}

def buildTime() {
    def df = new SimpleDateFormat("MMddhhmm") // you can change it
    df.setTimeZone(TimeZone.getTimeZone("UTC"))
    return df.format(new Date())
}

def buildYear() {
    def df = new SimpleDateFormat("YY") // you can change it
    df.setTimeZone(TimeZone.getTimeZone("UTC"))
    return df.format(new Date())
}

android {
    signingConfigs {
        config {
            keyAlias 'simonvn'
            keyPassword 'vaytah'
            storeFile file('/Volumes/Projects/simonpham.keystore')
            storePassword 'vaytah'
        }
    }
    compileSdkVersion 27
    buildToolsVersion '27.0.3'
    defaultConfig {
        applicationId "com.simonvn.tdtu.student"
        minSdkVersion 19
        targetSdkVersion 27
        versionCode 6
        versionName '3.0'
        vectorDrawables.useSupportLibrary = true
        signingConfig signingConfigs.config
    }
    buildTypes {
        android.applicationVariants.all { variant ->
            variant.outputs.all {
                outputFileName = "${variant.name}-${variant.versionName}-${buildTimestamp()}.apk"
            }
        }
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            versionNameSuffix ".${defaultConfig.versionCode}.r${buildYear()}"
        }
        debug {
            versionNameSuffix ".${defaultConfig.versionCode}.d${buildYear()}-${buildTime()}"
        }
        alpha {
            versionNameSuffix ".${defaultConfig.versionCode}.alpha${buildTime()}"
            debuggable true
        }
        repositories {
            maven { url 'https://oss.sonatype.org/content/repositories/ksoap2-android-releases' }
            maven { url 'https://jitpack.io' }
            jcenter()
        }
    }


    productFlavors {
    }
}
ext {
    supportLibVersion = '27.1.1'  // variable that can be referenced to keep support libs consistent
}
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "com.android.support:appcompat-v7:${supportLibVersion}"
    implementation "com.android.support:design:${supportLibVersion}"
    implementation "com.android.support:cardview-v7:${supportLibVersion}"
    implementation "com.android.support:recyclerview-v7:${supportLibVersion}"
    implementation "com.android.support:support-v4:${supportLibVersion}"
    implementation files('libs/activation.jar')
    implementation files('libs/additionnal.jar')
    implementation files('libs/mail.jar')
    implementation 'com.android.support.constraint:constraint-layout:1.1.0'
    implementation 'com.squareup.picasso:picasso:2.5.2'
    implementation 'org.jsoup:jsoup:1.11.2'
    implementation 'com.balysv:material-ripple:1.0.2'
    implementation 'com.jpardogo.materialtabstrip:library:1.1.1'
    implementation 'com.rengwuxian.materialedittext:library:2.1.4'
    implementation 'joda-time:joda-time:2.9.4'
    implementation 'com.wdullaer:materialdatetimepicker:2.4.0'
    implementation 'com.github.Kennyc1012:MultiStateView:1.3.0'
    implementation 'com.github.ybq:Endless-RecyclerView:1.0.3'
    implementation 'com.brandongogetap:stickyheaders:0.4.3'
    implementation 'com.google.code.ksoap2-android:ksoap2-android:3.6.2'
    implementation 'com.github.aakira:expandable-layout:1.6.0@aar'
    implementation 'com.blankj:utilcode:1.8.4'
    implementation 'com.github.curioustechizen.android-ago:library:1.3.4'
    implementation 'com.rm:rmswitch:1.2.2'
    implementation 'ru.alexbykov:nopermission:1.0.8'
    implementation 'com.github.clans:fab:1.6.4'
    implementation 'com.github.jkennethcarino:RTextEditorView:0.1.1'
    implementation 'com.jaredrummler:colorpicker:1.0.1'
    implementation 'com.nononsenseapps:filepicker:4.1.0'
    implementation 'com.google.firebase:firebase-database:11.8.0'
    testImplementation 'junit:junit:4.12'
}

apply plugin: 'com.google.gms.google-services'
